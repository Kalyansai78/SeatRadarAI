const logger = require("../utils/logger");

// Check whether bookings are released

async function isBookingReleased(page) {

}

// Wait until bookings are released

async function waitForBookingRelease(page) {

}

// Start monitoring booking release

async function monitorBookingRelease(page) {

}

module.exports = {
    isBookingReleased,
    waitForBookingRelease,
    monitorBookingRelease
};